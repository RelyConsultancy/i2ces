[
    {% for data in table_data %}
        {
            "metric": "{{ data.metric }}",
            "product": "{{ data.product }}",
            "results": "{{ data.results }}"
        }{{ not loop.last ? ',' }}
    {% endfor %}
]