[
    {% for data in table_data %}
        {
            "type": "{{ data.metric }}",
            "label": "{{ data.product }}",
            "value": "{{ data.results }}"
        }{{ not loop.last ? ',' }}
    {% endfor %}
]