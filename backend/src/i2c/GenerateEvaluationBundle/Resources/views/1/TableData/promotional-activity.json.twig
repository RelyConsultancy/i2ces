[
    {% for data in table_data %}
        {
            "date": "{{ data.start_date }}",
            "product": "{{ data.product }}",
            "results": "{{ data.results }}"
        }{{ not loop.last ? ',' }}
    {% endfor %}
]