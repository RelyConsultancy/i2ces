{% autoescape false %}
{
    "content": [
        {% for channel in channels %}
        {
            "label": "{{ channel.label }}",
            "type": "{{ channel.icon }}"
        }{{ not loop.last ? ',' }}
        {% endfor %}
    ],
    "is_additional_data": true,
    "serialized_name": "{{ additional_data.serialized_name }}",
    "title": "{{ additional_data.title }}",
    "state": "{{ additional_data.state }}",
    "created_at": "{{ date('now')|date("Y-m-d H:i:s") }}"
}
{% endautoescape %}