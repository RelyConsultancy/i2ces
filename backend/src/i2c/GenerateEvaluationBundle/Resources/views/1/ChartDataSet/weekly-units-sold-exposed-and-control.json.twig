{% autoescape false %}
[
    {% for item in data %}
    {
        "date_start": "{{ item.start_date }}",
        "exposed": "{{ item.exposed }}",
        "control": "{{ item.control }}"
    }{{ not loop.last ? ',' }}
    {% endfor %}
]
{% endautoescape %}